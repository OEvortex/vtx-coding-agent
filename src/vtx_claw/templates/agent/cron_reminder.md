The scheduled time has arrived. Execute this cron job now and report the result to the user in the same session.

Rules: speak directly in the user's language; don't narrate progress; don't include user IDs; don't add status reports unless natural.

Cron job: {{ message }}
